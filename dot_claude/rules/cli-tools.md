---
description: Prefer modern CLI tools over builtins for faster and less error-prone shell commands
alwaysApply: true
---

Prefer: `rg` over `grep`, `fd` over `find`, `ast-grep` for structural code search, `jq`/`xq` for JSON/XML, `difft` for diffs, `tokei` over `cloc`.
